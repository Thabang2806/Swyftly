# Codex Prompt Progress

Source prompt pack: `Documentation/swyftly_codex_development_prompts.md`

Last updated: 2026-05-21

## How To Use This Tracker

- Keep the source prompt pack unchanged.
- Update this file after every completed implementation task.
- Use `Done` only when the prompt acceptance criteria are met.
- Use `Partial` when foundation work exists but the prompt still has unmet acceptance criteria.
- Add short evidence: changed area, verification command, or known blocker.
- Keep ad hoc work in the separate section at the bottom.

## Current Snapshot

- Done: 68
- Partial: 0
- Not started: 2
- Current recommended next step: PayFast/payment work is paused. The next non-payment candidates are carrier integration design, address verification/pickup points, SMS/push or real-time notification channels, or secure payout-bank re-verification.

## Prompt Checklist

| # | Prompt | Status | Evidence / next action |
|---|---|---|---|
| 1 | Create the initial monorepo | Done | Backend solution, Angular SSR app, root docs, Docker compose, `.env.example`, README, and project folders created. |
| 2 | Create AGENTS.md for Codex guidance | Done | `AGENTS.md` added with stack, architecture, security, payment, AI, and command guidance. |
| 3 | Add development environment and health checks | Done | API `/health`, CORS, logging, Angular shell routes, and frontend build verified. Docker runtime not verified because Docker is unavailable locally. |
| 4 | Write architecture.md | Done | `docs/architecture.md` added. |
| 5 | Write feature roadmap documentation | Done | `docs/feature-roadmap.md` added. |
| 6 | Write coding standards documentation | Done | `docs/coding-standards.md` added. |
| 7 | Configure PostgreSQL and EF Core | Done | EF tooling, design-time factory, initial schema migration, `/health/ready`, and opt-in PostgreSQL tests exist. Migration applied to local PostgreSQL and DB readiness/tests passed. |
| 8 | Add common backend building blocks | Done | Added entity/domain-event primitives, result/error and validation types, current-user abstraction, auditable timestamp interceptor, result-to-ProblemDetails mapping, audit placeholder, and tests. No new migration. |
| 9 | Add CI pipeline | Done | Added GitHub Actions CI with backend restore/build/test, PostgreSQL integration tests, frontend npm ci/build/test, and README mention. |
| 10 | Implement identity and roles | Done | Added ASP.NET Core Identity, JWT access tokens, hashed refresh tokens, buyer/seller profiles, role policies, auth endpoints, Identity migrations, docs, and auth integration tests. Build/tests passed; migrations applied to local PostgreSQL and opt-in PostgreSQL tests passed with local connection. |
| 11 | Implement Angular auth screens and guards | Done | Added Angular login, buyer/seller registration, session-scoped auth service, JWT interceptor, current-user loading, logout, role guards, navigation updates, and tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed. |
| 12 | Implement seller profile and verification domain | Done | Added seller profile fields, UnderReview status, storefront/address/verification/payout placeholder entities, EF configurations, migration, and status-rule unit tests. Build/tests passed; migration applied to local PostgreSQL and opt-in PostgreSQL tests passed. |
| 13 | Seller onboarding API | Done | Added seller-only onboarding endpoints, current-user ownership, profile/storefront/address/payout placeholder updates, verification submission to UnderReview, validation, slug conflict handling, API docs, and integration tests. Build/test passed. |
| 14 | Angular seller onboarding wizard | Done | Added seller onboarding wizard on `/seller`, API client, profile/storefront/address/payout/review steps, verification status display, validation, submit flow, lazy route loading, and Angular tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed. |
| 15 | Admin seller approval | Done | Added admin seller review endpoints, approval/rejection/suspension transitions, persisted seller audit logs, Angular admin review pages, migration, and tests. Build/test passed; migration applied to local PostgreSQL and opt-in PostgreSQL tests passed. |
| 16 | Implement categories and category attributes | Done | Added category hierarchy and category-attribute domain models, seeded initial fashion/beauty/accessory taxonomy, admin category listing API, EF migration, validation rules for required/allowed attributes, and tests. Build/test passed; migration applied to local PostgreSQL and opt-in PostgreSQL tests passed. |
| 17 | Implement product aggregate and statuses | Done | Added product aggregate, Draft/PendingReview/Published/Rejected/Archived/OutOfStock statuses, seller edit rules, admin-only publishing transition, seller-scoped slug uniqueness, EF migration, and unit tests. Build/test passed; migration applied to local PostgreSQL and opt-in PostgreSQL tests passed. |
| 18 | Implement product variants | Done | Added product variant model/statuses, price/stock/reservation rules, seller draft variant commands, EF mapping, migration, and tests. |
| 19 | Implement product images | Done | Added product image records, primary-image rule, storage provider abstraction with development placeholder, seller image attach endpoint, EF mapping, migration, and tests. |
| 20 | Seller product draft API | Done | Added seller product draft endpoints, ownership checks, verified-seller submit rule, category attribute validation, variant/image submission validation, and integration tests. Build/test passed; migration applied to local PostgreSQL and opt-in PostgreSQL tests passed. |
| 21 | Angular seller product form | Done | Added seller product list, create/edit form, dynamic category attributes, variant/image add/remove flows, review submit state, route wiring, seller catalog category endpoint support, and Angular tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed. |
| 22 | Define AI product suggestion schema and database tables | Done | Added AI suggestion, field audit, usage log, prompt version entities/DTOs, EF mappings, JSONB tables, migration, and tests. Build/test passed; migration applied to local PostgreSQL and opt-in PostgreSQL tests passed. |
| 23 | Implement AI Listing Assistant service abstraction | Done | Added AI listing assistant service contract/implementation, provider abstraction, prompt builder, validator, usage logger, local fake provider, DI registration, and unit tests. Build/test passed. |
| 24 | Implement AI product suggestion endpoint | Done | Added `POST /api/seller/products/{productId}/ai-suggestions`, seller ownership/editability checks, image/category validation, fake-provider generation, persistence, usage logging, API docs, and integration tests. Build/test passed. |
| 25 | Apply AI suggestions to product draft | Done | Added `POST /api/seller/products/{productId}/ai-suggestions/{suggestionId}/apply`, selected-field application, seller edits, risk confirmation guard, attribute/image ownership validation, product tags JSONB migration, field audits, docs, and integration tests. Build/test passed; migration applied locally and opt-in PostgreSQL tests passed with explicit local connection. |
| 26 | Angular AI Product Listing Assistant panel | Done | Added AI Product Assistant panel to seller product editor, generate/apply service calls, reactive review/edit forms, field-by-field apply controls, risk/missing-field display, AI styling, and Angular tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed. |
| 27 | AI moderation rules for product listings | Done | Added product moderation service, counterfeit/beauty/missing-field business rules, `AiModerationResult` table, `NeedsAdminReview` product status, submit-review moderation hook, migration, docs, and unit/integration tests. Build/test passed; migration applied locally and opt-in PostgreSQL tests passed with explicit local connection. |
| 28 | Admin product review workflow | Done | Added admin product review endpoints, product approval/rejection/change-request transitions, high-risk override rule, product audit logs, Angular admin product queue/detail pages, and tests. Backend build/test and Angular build/test passed. |
| 29 | Admin audit logs | Done | Added shared audit logging service, refactored seller/product admin workflows to use it, added filtered `GET /api/admin/audit-logs`, Angular audit-log page, docs, and tests. Backend build/test and Angular build/test passed. |
| 30 | Implement basic product search API | Done | Added PostgreSQL-backed `GET /api/products/search`, public product/category/seller detail endpoints, published-only visibility, variant/attribute filters, sorting, pagination, docs, and integration tests. Backend build/test passed. |
| 31 | Angular shop and category pages | Done | Added `/shop`, `/category/:slug`, `/product/:slug`, `/seller/:storeSlug`, public catalog service, product cards, filters, sort, empty/loading states, docs, and Angular tests. Frontend build/test passed. |
| 32 | Add Typesense or Meilisearch integration placeholder | Done | Added `ISearchIndexService`, `IProductSearchIndexer`, `ProductSearchDocument`, local in-memory placeholder indexing, publish-time product indexing, PostgreSQL search fallback, docs, and tests. Backend build/test passed. |
| 33 | Add product embeddings and pgvector foundation | Done | Added `ProductEmbedding`, pgvector EF mapping, `ProductEmbeddingsPgvector` migration, embedding contracts, fake provider, publish-time generator, docs, and tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, local `dotnet dotnet-ef database update`, and opt-in PostgreSQL tests passed after pgvector was installed on PostgreSQL. |
| 34 | Implement single-seller cart | Done | Added `Cart` and `CartItem`, buyer-only `/api/cart` endpoints, one-active-cart and single-seller rules, stock validation against variant available quantity, captured display pricing, EF migration, docs, and tests. Build/test passed; migration applied locally and opt-in PostgreSQL tests passed. |
| 35 | Implement inventory reservations | Done | Added `InventoryReservation`, reservation statuses, `IInventoryReservationService`, EF reservation service with transactional cart reservation and expiry release, worker expiry placeholder, migration, docs, and tests. Build/test passed; migration applied locally and opt-in PostgreSQL tests passed. |
| 36 | Create order aggregate from cart | Done | Added order aggregate/statuses/status history, order creation service from active cart, inventory reservation hook, idempotent pending-payment order creation, buyer/seller order endpoints, EF migration, docs, and tests. Build/test passed; migration applied locally and opt-in PostgreSQL tests passed with the local API connection string. |
| 37 | Angular cart and checkout UI | Done | Added Angular cart service/models, `/cart`, `/checkout`, `/checkout/success`, `/checkout/failed`, buyer cart nav, product-detail add-to-cart, quantity update/remove flows, checkout shipping placeholder, backend order creation call, docs, and tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed. |
| 38 | Payment provider abstraction | Done | Added `IPaymentProvider`, payment initiation/verification/webhook DTOs, `PaymentProviderOptions`, `IPaymentInitiationService`, fake Infrastructure provider with success/failure simulation and optional webhook signature verification, non-secret config defaults, docs, and tests. Build/test passed; EF model drift check showed no migration needed. |
| 39 | Implement payments and payment events | Done | Added `Payment` and `PaymentEvent` entities/statuses, local payment initiation flow, buyer-only `POST /api/payments/initiate`, provider reference storage, migration, docs, and tests. Fake provider success/failure paths covered. |
| 40 | Payment webhook idempotency | Done | Added anonymous `POST /api/payments/webhook/{provider}`, provider signature verification hook, raw event storage, `(Provider, ProviderEventId)` idempotency, paid/failed webhook processing, order/payment/reservation updates, duplicate-event test coverage, and docs. |
| 41 | Internal marketplace ledger | Done | Added `LedgerEntry`, `SellerBalance`, `CommissionRule`, fee configuration, successful-payment ledger service, seller pending balance crediting, migration, docs, and tests for commission calculation and ledger idempotency. |
| 42 | Seller balances and payout workflow | Done | Added seller payout records/items, seller balance and payout read APIs, admin pending/hold/release payout endpoints, pending-to-held balance transitions, payout audit logs, migration, docs, and tests. Build/test, EF drift check, local migration update, and opt-in PostgreSQL tests passed. |
| 43 | Manual shipping and fulfilment | Done | Added Shipment/ShipmentEvent entities, seller mark-processing/tracking/mark-shipped endpoints, buyer `/api/buyer/orders` aliases, order responses with shipment tracking, EF migration, docs, authorization/status-transition tests, local migration update, EF drift check, and opt-in PostgreSQL tests. Phase 8A later added seller delivery confirmation on top of this fulfilment foundation. |
| 44 | Return request workflow | Done | Added ReturnRequest/ReturnItem/ReturnMessage entities and statuses/reasons, buyer return/dispute endpoints, seller return approve/reject endpoints, admin disputed-return listing, return-triggered payout holds, EF migration, docs, unit/integration tests, local migration update, EF drift check, and opt-in PostgreSQL tests. Refunds and final dispute resolution remain future prompts. |
| 45 | Refund workflow and ledger reversals | Done | Added Refund/RefundEvent entities and statuses, payment-provider refund contract/fake provider support, admin refund create/approve endpoints, proportional ledger reversal entries, seller balance refund debit including negative-balance recovery signal, payment/order refund status updates, audit logs, EF migration, docs, full/partial refund tests, local migration update, EF drift check, and opt-in PostgreSQL tests. |
| 46 | Dispute workflow | Done | Added Dispute/DisputeMessage/DisputeEvidence entities and statuses, buyer order/return dispute endpoints, buyer/seller messages and evidence, admin dispute listing/resolution, dispute-triggered payout holds, seller-favoured payout release, buyer-favoured hold retention for refund/recovery, audit logs, EF migration, docs, unit/integration tests, local migration update, EF drift check, and opt-in PostgreSQL tests. |
| 47 | Admin dashboard foundation | Done | Added admin-only `GET /api/admin/dashboard/summary`, aggregate operational metrics, Angular `/admin` dashboard, `/admin/sellers` queue route, protected admin section placeholders, docs, and tests. Backend build/test and Angular build/test passed. |
| 48 | Support ticket system | Done | Added SupportTicket/SupportMessage entities, categories/statuses, buyer/seller create/read/message endpoints, support/admin read/respond/internal-note/resolve/close endpoints, private internal-note filtering, EF migration, docs, unit/integration tests, local migration update, EF drift check, and opt-in PostgreSQL tests. |
| 49 | Ad campaign domain model | Done | Added ad campaign, campaign product, budget, impression, click, conversion, charge, and seller ad credit models; EF mappings; eligibility service with verified-seller, published/in-stock product, moderation, quality-score, suspension, and serious-dispute rules; migration; docs; and tests. |
| 50 | Seller ad campaign API | Done | Added seller-owned ad campaign create/list/detail/update/submit-review/pause/resume/cancel endpoints, budget handling, eligibility validation, ownership checks, docs, integration tests, local migration update, EF drift check, and opt-in PostgreSQL tests. |
| 51 | Admin ad campaign review | Done | Added admin campaign pending/detail/approve/reject API, approval eligibility recheck, campaign audit logs, Angular admin ad queue/detail pages, docs, and tests. Backend build/test, EF drift check, Angular build/test passed. |
| 52 | Track ad impressions, clicks, and conversions | Done | Added anonymous impression/click capture, active/in-flight/published/in-stock tracking rules, click spend charges with budget limits, seller campaign metrics query, paid-order conversion attribution hook, docs, tests, backend build/test, and EF drift check. |
| 53 | Seller ad campaign dashboard UI | Done | Added Angular seller ad campaign service/models, `/seller/ads`, `/seller/ads/new`, `/seller/ads/:id`, campaign list/create/detail flows, product selection, eligibility warnings, metrics cards, pause/resume/cancel/submit actions, docs, and tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed. |
| 54 | Seller analytics dashboard | Done | Added seller-only analytics summary endpoint, aggregate sales/order/AOV/product/refund/return/ad/AI metrics, low-stock and top-product tables, Angular `/seller/analytics` dashboard, seller workspace link, docs, seller-isolation integration tests, Angular tests, backend/frontend build/test, and EF drift check. |
| 55 | Admin finance and marketplace reports | Done | Added admin-only marketplace report API, aggregate CSV export, Angular `/admin/reports` page, docs, admin access tests, backend/frontend build/test, and EF drift check. |
| 56 | AI usage analytics | Done | Added admin-only AI usage analytics API, Angular `/admin/ai-usage` dashboard, seller AI benefit metrics, docs, and tests. Quality score improvement is nullable because pre-AI baseline scores are not captured yet. |
| 57 | Add API authorization audit | Done | Added `docs/authorization-audit.md` and integration tests covering anonymous/seller/buyer/admin role boundaries plus buyer/seller ownership isolation. |
| 58 | Add rate limiting and abuse protection placeholders | Done | Added ASP.NET Core fixed-window rate-limit policies for auth, AI, product writes, payment initiation, ad clicks, and search; configurable defaults; 429 JSON response; docs; and a search policy integration test. |
| 59 | Add webhook security and idempotency review | Done | Required fake-provider webhook signing secret, rejected invalid/misconfigured signatures before event persistence, kept duplicate webhook idempotency tests, added invalid-signature payment-service test, and documented webhook signature/idempotency behavior. |
| 60 | Add observability foundation | Done | Added correlation ID middleware, request/error logging middleware, provider placeholder readiness checks, `Swyftly.Api` meter counters, docs, and correlation ID integration test. |
| 61 | Buyer AI shopping assistant intent extraction | Done | Added `IAiShoppingIntentService`, intent DTOs, fake intent provider, DI registration, docs, and unit tests for fashion, jewellery, beauty, blank, and vague buyer prompts. No product recommendation endpoint yet. |
| 62 | Buyer AI shopping assistant product recommendations | Done | Added buyer-only shopping assistant endpoint, backend product search over published in-stock products, deterministic match reasons, Angular `/assistant` page with product cards, docs, and tests verifying only real product IDs are returned. |
| 63 | Visual search MVP | Done | Added buyer-only visual search endpoint, Application vision contracts, fake vision provider, transient image validation, Angular `/visual-search` page with upload/reference flow, docs, and tests verifying only real published in-stock product ids are returned. |
| 64 | Codex PR review prompt | Not started | Use when reviewing a PR. |
| 65 | Security review prompt | Done | Added `docs/security-review.md` with prioritized findings. The high findings were remediated in the 2026-05-19 security pass; medium findings remain for follow-up. |
| 66 | Payment and ledger review prompt | Done | Added `docs/payment-ledger-review.md` with blocking finance findings, including duplicate payment initiation and non-idempotent refund approval. |
| 67 | Refactor prompt template | Not started | Use for scoped refactoring tasks. |
| 68 | Bug fix prompt template | Done | Used for finance payment/refund bug fixes: idempotent payment initiation, safer payment webhook status transitions, refund approval processing/idempotency guards, `FinancePaymentRefundIdempotency` migration, tests, local migration update, and opt-in PostgreSQL verification. |
| 69 | Documentation update prompt | Done | Reviewed and updated `docs/architecture.md`, `docs/database-schema.md`, `docs/api-contracts.md`, `docs/feature-roadmap.md`, and `docs/coding-standards.md` after the latest visual-search and review work. |

## Ad Hoc Work Completed

| Date | Work | Evidence |
|---|---|---|
| 2026-05-18 | Added Swagger/OpenAPI to API project | `Swashbuckle.AspNetCore` added, `/swagger` enabled in Development, build/test passed, runtime Swagger JSON checked. |
| 2026-05-18 | Consolidated git ignores | Single root `.gitignore`, nested Angular `.gitignore` removed, `.vscode/`, `.env`, frontend build outputs, and Angular cache ignored. |
| 2026-05-19 | Remediated high security review findings | Removed usable production defaults from root appsettings, added production secret/startup validation, login lockout tracking, ad-impression and webhook rate limits, webhook JSON/size guards, anonymous impression fingerprint dedupe, docs, and focused tests. |
| 2026-05-19 | Remediated next security/finance gaps | Added refresh-token family replay revocation, public storefront/catalog verified-published seller filtering, conditional inventory reservation updates, reserved-stock check constraints, `RefreshTokenFamilyAndInventoryGuards` migration, docs, and focused tests. |
| 2026-05-19 | Remediated finance, webhook, and cookie auth gaps | Added duplicate webhook race fallback, refund payout adjustments, fake-provider payout lifecycle, finance roles/policies with dual control, HttpOnly refresh-token cookies with CSRF, Angular in-memory access tokens, `FinancePayoutLifecycleAndCookieAuth` migration, docs, backend/frontend tests, local migration update, and opt-in PostgreSQL verification. |
| 2026-05-19 | Implemented Phase 5C admin moderation UI polish | Added shared admin workspace navigation, seller/product queue filters, denser moderation rows, seller completeness indicators, product image review/fallbacks, AI risk display polish, audit-log UI polish, docs, and Angular tests. `cmd /c npm run test:ci` passed with 133 specs. |
| 2026-05-19 | Implemented Phase 6A buyer account operations UI | Added buyer workspace navigation, account dashboard summaries, buyer order list/detail with delivered-order return request form, buyer return list/detail with dispute escalation, buyer disputes inline case handling, buyer support ticket list/create/detail/message flows, docs, and Angular tests. `cmd /c npm run build` passed with the existing bundle budget warning; `cmd /c npm run test:ci` passed with 143 specs. |
| 2026-05-19 | Implemented Phase 6B buyer AI discovery polish | Reworked `/assistant` and `/visual-search` with shared UI primitives, example prompts, extracted intent/attribute panels, improved product result cards, upload preview, image type/size validation, docs, and Angular tests. `cmd /c npm run build` passed with the existing bundle budget warning at 597.22 kB initial total; `cmd /c npm run test:ci` passed with 148 specs. |
| 2026-05-19 | Implemented Phase 6C frontend bundle hygiene | Removed Angular Material button/toolbar imports from the root app shell, replaced root navigation with styled native links/buttons, recalibrated the initial bundle warning budget to `650kB`, and documented that sub-500kB requires a broader global-style split. `cmd /c npm run build` passed with no budget warning at 593.96 kB initial raw / 140.80 kB estimated transfer; `cmd /c npm run test:ci` passed with 148 specs. |
| 2026-05-19 | Implemented Phase 5D admin order/payment read operations | Added FinanceRead-protected `GET /api/admin/orders`, `GET /api/admin/orders/{orderId}`, `GET /api/admin/payments`, and `GET /api/admin/payments/{paymentId}`; replaced admin API-gap pages with Angular order/payment list/detail screens; removed the stale gap component; updated docs; and added backend/frontend tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. |
| 2026-05-19 | Implemented Phase 7A buyer engagement backend foundation | Added wishlist, verified-buyer review, public review summary/list, and in-app notification persistence/APIs; created `BuyerEngagementFoundation` migration; added notification service foundation, docs, and backend tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF model drift check, local migration update, and opt-in PostgreSQL tests passed. |
| 2026-05-19 | Implemented Phase 7B buyer engagement UI | Added Angular buyer wishlist, reviews, and notifications routes; wired buyer workspace/account navigation; enabled product-card/product-detail wishlist saves; added public review summary/list display on product detail; added delivered-order review creation; updated docs and frontend tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed. |
| 2026-05-19 | Implemented Phase 7C review moderation and notifications | Added moderation-first verified-buyer reviews, admin review moderation APIs/UI, moderation metadata migration, audit logs, in-app notifications for review decisions, return responses, support replies, tracking updates, and shipped orders, plus docs and tests. Backend build/test, EF drift check, Angular build, and Angular tests passed. |
| 2026-05-19 | Implemented Phase 8A checkout closure | Added persisted payment checkout URLs, retryable pending-payment flows, paid-webhook cart cleanup, seller mark-delivered endpoint/UI, delivery notifications, `CheckoutPaymentRetryAndDeliveryConfirmation` migration, docs, and backend/frontend tests. |
| 2026-05-19 | Completed Phase 8B payment provider comparison spike | Added PayFast vs PayU South Africa comparison docs, provider implementation notes, non-secret provider config placeholders, and API contract notes. Runtime provider remains `Fake`; no provider adapter, real provider calls, or migration added. |
| 2026-05-19 | Implemented Phase 8C PayFast payment adapter foundation | Added PayFast provider selection, hosted-checkout bridge/form generation, PayFast ITN parsing/signature/remote-validation support, amount/currency settlement guards, manual PayFast refund confirmation endpoint, production provider config validation, docs, and focused tests. Backend build/test, EF pending-model check, Angular build, and Angular test suite passed. Fake remains the default provider outside explicit PayFast configuration; no migration was added. |
| 2026-05-19 | Implemented Phase 8D PayFast sandbox readiness | Replaced the generic payment-provider readiness placeholder with a selected-provider health check, added PayFast config readiness tests, documented the sandbox verification runbook, and updated observability/API docs. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, and EF pending-model check passed. No provider status-query API, automatic refund API, or migration was added because live PayFast sandbox evidence is still required. |
| 2026-05-19 | Implemented Phase 8E buyer-favoured dispute refund handoff | Buyer-favoured admin dispute resolution now validates a refundable paid payment, creates a requested refund for the remaining refundable amount, keeps linked payout funds held, and leaves provider execution under the existing finance approval/manual PayFast confirmation flow. Updated finance docs and dispute tests. `dotnet test backend\Swyftly.sln --no-build` and EF pending-model check passed. |
| 2026-05-19 | Implemented Phase 8F seller payout response minimization | Split seller and admin payout response shapes so seller payout history no longer returns internal ledger, order, or payment identifiers, while admin finance pending-payout reads retain reconciliation identifiers. Updated seller payout UI/tests and finance docs. `dotnet build backend\Swyftly.sln --no-restore`, focused `PayoutTests`, full `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. |
| 2026-05-19 | Implemented Phase 8G payment reconciliation candidates | Added a FinanceRead `GET /api/admin/payments/reconciliation-candidates` queue for stale pending/authorized payments and failed webhook events, surfaced it on the admin payments page, and documented that it is read-only operational triage rather than provider status-query reconciliation. `dotnet build backend\Swyftly.sln --no-restore`, focused `AdminOrderPaymentTests`, full `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. |
| 2026-05-19 | Implemented Phase 8H payment webhook payload hygiene | Normalized persisted payment webhook payloads into sanitized JSON, including PayFast form ITNs, and redacted signatures/tokens/secrets/passwords/passphrases/card fields before storage. Updated docs and tests. `dotnet build backend\Swyftly.sln --no-restore`, focused `PaymentServiceTests|PaymentEndpointTests`, full `dotnet test backend\Swyftly.sln --no-build`, and EF pending-model check passed. |
| 2026-05-19 | Implemented Phase 8I Angular bearer-token origin hardening | Replaced prefix-based bearer-token attachment with parsed URL origin/base-path checks and added a deceptive-host-prefix regression test. Updated security docs. `cmd /c npm run build` and `cmd /c npm run test:ci` passed. |
| 2026-05-19 | Implemented Phase 8J auth-cookie configuration hardening | Added centralized `AuthCookies` configuration for refresh/CSRF cookie path, domain, SameSite, and Secure attributes; wired auth endpoints through those options; added production validation and emitted-cookie integration tests; and updated docs. |
| 2026-05-19 | Implemented Phase 8K payment webhook payload retention | Added raw webhook payload redaction retention for `payment_events`, `PaymentWebhookPayloadRetention` config, worker cleanup wiring, `PaymentWebhookPayloadRetention` migration, docs, and unit tests. `dotnet build backend\Swyftly.sln --no-restore`, focused retention test, full `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, and local migration update passed. |
| 2026-05-21 | Implemented high-fidelity UI HF-1 foundation | Added `docs/ui-high-fidelity-progress.md`, linked it from the UI roadmap, refreshed Luxe Blush tokens, upgraded the app shell/search/mobile bottom navigation, added shared high-fidelity visual primitives, and updated product-card fallback visuals. `cmd /c npm run build` and `cmd /c npm run test:ci` passed with 183 specs. |
| 2026-05-21 | Implemented high-fidelity UI HF-2 buyer public screens | Aligned `/`, `/shop`, `/category/:slug`, and `/product/:slug` to the Luxe Blush mockup direction with a buyer-first home hero, presentation-only style cue cards, richer shop/category headers and filters, larger product gallery, thumbnail rail, variant pills, purchase/trust panels, docs tracking, and Angular tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed with 184 specs. |
| 2026-05-21 | Implemented high-fidelity UI HF-3 checkout and AI assistant | Aligned `/checkout`, `/checkout/success`, `/checkout/failed`, and `/assistant` to the Luxe Blush mockup direction with a compact checkout hero/progress, delivery/payment cards, stronger order summary/trust strip, polished result cards, chat-style AI prompt panel, extracted intent card, recommendation grid, product fallbacks, docs tracking, and Angular tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed with 184 specs. |
| 2026-05-21 | Implemented high-fidelity UI HF-4 seller workspace screens | Aligned `/seller`, the seller product editor AI panel, and `/seller/ads` to the Luxe Blush mockup direction with a seller workspace shell, dark side navigation, verified-seller dashboard metrics, AI listing preview/suggestion panel, ad campaign metric tiles, dense campaign rows, budget progress, docs tracking, and Angular tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed with 184 specs; mojibake scan returned no matches. |
| 2026-05-21 | Implemented high-fidelity UI HF-5 admin moderation and finance payouts | Aligned `/admin/products`, `/admin/sellers`, `/admin/reviews`, and `/admin/payouts` to the Luxe Blush mockup direction with a plum admin workspace nav, queue-derived metric tiles, dense moderation/finance queues, selected evidence panels, payout ledger snapshot, role-aware action panel, docs tracking, and Angular tests. `cmd /c npm run build` and `cmd /c npm run test:ci` passed with 184 specs; mojibake scan returned no matches. |
| 2026-05-21 | Implemented Phase 9A seller inventory and store settings | Added seller inventory APIs for owned variant listing and audited stock/status adjustment, dedicated Angular `/seller/inventory` route, seller `/seller/settings/store` route using existing onboarding APIs, seller navigation/dashboard links, docs, and backend/frontend tests. Backend build/tests and EF drift check passed; Angular build and spec TypeScript compile passed. Karma was blocked by local Chrome paging-file failure before specs ran. |
| 2026-05-21 | Implemented Phase 9B PayFast reconciliation and manual refund hardening | Added `payment_reconciliation_reviews`, FinanceApprove review recording, latest-review/snooze support on payment reconciliation candidates, admin payments review evidence UI, clearer PayFast manual refund confirmation UI, `PaymentReconciliationReviews` migration, docs, and backend/frontend test updates. Backend build/tests and EF drift check passed; Angular build and spec TypeScript compile passed. Local PostgreSQL migration apply was blocked because `localhost:5432` refused the connection, and Karma exited before browser test execution on this machine. |
| 2026-05-21 | Implemented Phase 9C admin catalog and attribute management | Added admin category/attribute create, update, activate, and deactivate APIs with no hard delete, parent-cycle and duplicate guards, breaking attribute-edit guards, audit logs, an API-backed `/admin/categories` workspace, docs, and backend/frontend tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. No migration was needed. |
| 2026-05-21 | Implemented Phase 9D seller product editor UX polish | Added seller-owned product image metadata update API, primary-image switching, editable-status guards, product editor workspace navigation, status/read-only banners, step completion, visual image gallery, image metadata form, variant edit mode, stock badges, honest shipping guidance, buyer-facing review preview, docs, and backend/frontend tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. No migration was needed. |
| 2026-05-21 | Implemented Phase 9E buyer wishlist and saved-for-later cart polish | Added wishlist product-id hydration, wishlist variant options, atomic wishlist-to-cart and cart-to-wishlist bridge endpoints, cart product slug/image metadata, cached Angular wishlist state, saved-state toggles on product cards/detail, wishlist move-to-cart controls, cart product imagery/detail links/save-for-later actions, docs, and backend/frontend tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. No migration was needed. |
| 2026-05-21 | Implemented Phase 9F seller image upload and published listing revisions | Added local API-managed image uploads, ignored local media storage, multipart seller image upload endpoints, published-product listing revision persistence, seller revision APIs, admin pending-revision moderation APIs, admin revision detail UI, seller product editor file-upload/revision mode, `SellerImageUploadAndProductRevisions` migration, docs, and backend/frontend tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. Local migration apply was attempted but blocked because `localhost:5432` refused the connection. |
| 2026-05-21 | Implemented Phase 9G buyer profile settings and in-app notification preferences | Added buyer profile display/phone settings, category-level in-app notification preferences for Orders/Returns/Reviews/Support, suppression of future known notification types when muted, `/account/settings`, buyer settings service/routes/nav/docs, and `BuyerProfileSettingsAndNotificationPreferences` migration. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. Local migration apply was attempted but blocked because `localhost:5432` refused the connection. |
| 2026-05-21 | Implemented Phase 9H saved delivery addresses and order snapshots | Added buyer saved delivery-address APIs, default-address behavior, order delivery-address snapshots, checkout saved/inline address selection, buyer/seller order detail address display, `/account/settings` address management, `BuyerDeliveryAddressesAndOrderDeliverySnapshot` migration, docs, and backend/frontend tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, local migration update, opt-in PostgreSQL tests with the configured local connection, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. PayFast sandbox verification remains blocked until sandbox credentials and a public callback URL are available. |
| 2026-05-21 | Implemented Phase 9I production media storage hardening | Added media asset and variant metadata, nullable media links for product and revision images, S3-compatible storage provider scaffolding, SkiaSharp-backed image decode/validation/WebP variant generation, pluggable media scanner with local clean default, image-storage readiness checks, worker media cleanup, `ProductionMediaStorageHardening` migration, docs, and focused media tests. `dotnet restore backend\Swyftly.sln`, `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, local migration update, opt-in PostgreSQL tests with the configured local connection, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. PayFast sandbox verification remains blocked until sandbox credentials and a public callback URL are available. |
| 2026-05-21 | Implemented Phase 9J delivery instructions and fulfilment exceptions | Added optional delivery instructions to buyer saved addresses and order snapshots, seller ready-to-ship/delivery-failed/returned-to-sender endpoints, manual fulfilment exception events, buyer notifications for new fulfilment states, checkout/settings/order-detail UI updates, admin order delivery context, `DeliveryInstructionsAndFulfilmentExceptions` migration, docs, and backend/frontend tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, local migration update, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. PayFast/payment work remains intentionally paused. |
| 2026-05-21 | Implemented Phase 9K seller bulk inventory import/export | Added seller inventory CSV export/template endpoints, CSV import preview, all-or-nothing bulk stock/status apply with per-row validation and audit logs, Angular `/seller/inventory` bulk import/export UI, service/model/spec updates, and docs. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. No migration was added. PayFast/payment work remains intentionally paused. |
| 2026-05-21 | Implemented Phase 9L seller delivery methods and checkout shipping rates | Added `seller_delivery_methods`, seller delivery-method CRUD/activate/deactivate APIs with audit logs, buyer cart shipping-options quotes, server-side shipping recomputation during order creation, order delivery-method/rate snapshots, Angular seller store settings delivery-method management, checkout delivery option selection/totals, order detail delivery-method display, `SellerDeliveryMethodsAndShippingRates` migration, docs, and backend/frontend tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, local migration update, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. PayFast/payment work remains intentionally paused. |
| 2026-05-21 | Implemented Phase 9M buyer email notification delivery foundation | Added channel-aware buyer notification preferences, hidden in-app notification rows for email-only delivery, durable `notification_email_deliveries` outbox, `LogOnly` and SMTP email providers, worker email processing with retry/failure tracking, email readiness/production config validation, `/account/settings` in-app/email toggles, `BuyerEmailNotificationDelivery` migration, docs, and backend/frontend tests. `dotnet build backend\Swyftly.sln --no-restore`, `dotnet test backend\Swyftly.sln --no-build`, EF pending-model check, local migration update, `cmd /c npm run build`, and `cmd /c npm run test:ci` passed. PayFast/payment work remains intentionally paused. |
